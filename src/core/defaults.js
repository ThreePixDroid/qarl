import { easings } from '../behaviors/easings'

export const DEFAULTS = {
  time: 0,
  loop: false,
  mode: null,
  delay: 0,
  repeat: 0,
  target: null,
  easing: easings.linear,
  reversed: false,
  repeatDelay: 0,
}